# Nursing AI Assistant — Python dependencies
# Tested with Python 3.13 on Windows.
#
# Install with:   pip install -r requirements.txt
# (setup.bat does this for you inside a local .venv)

# --- UI ---
streamlit==1.57.0

# --- LangChain stack (pinned: these APIs change often) ---
langchain==1.2.17
langchain-core==1.3.3
langchain-community==0.4.1
langchain-classic==1.0.6
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2

# --- Vector store + retrieval ---
chromadb==1.5.9
rank-bm25==0.2.2

# --- Embeddings / ML ---
# NOTE on torch: the line below installs the CPU build, which works everywhere
# (just slower). For an NVIDIA GPU, instead run:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
transformers==5.8.0
sentence-transformers==5.4.1
huggingface-hub>=0.30

# --- Document ingestion (PDF / DOCX / images) ---
PyMuPDF==1.27.2.2
pdfplumber==0.11.0
python-docx==1.1.0
pillow==12.2.0

# --- Question bank export ---
pandas==2.2.3
openpyxl==3.1.2

# --- Voice ---
# Speech-to-text uses the transformers Whisper pipeline (above).
# soundfile lets it decode the recorded .wav; edge-tts / pyttsx3 do text-to-speech.
soundfile==0.12.1
edge-tts==7.2.8
pyttsx3==2.90
nest-asyncio==1.6.0

# --- LLM / vision via Ollama ---
ollama==0.6.2
